{
  "name": "calendar-app-monorepo",
  "version": "1.0.0",
  "description": "Calendar application with frontend and backend",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "concurrently \"cd calendar-app && npm run dev\" \"cd calendar-app/backend && npm run dev\"",
    "build": "cd calendar-app && npm run build",
    "install-all": "npm install && cd calendar-app && npm install && cd backend && npm install",
    "render-build": "npm run install-all && npm run build"
  },
  "keywords": [
    "calendar",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
